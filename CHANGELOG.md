# Changelog

## 0.9.1 — 2026-09-02

### 修复

- 管理员启动现在能够识别 UAC 取消和 `Start-Process` 启动失败，不再误记为已启动。
- 修复带引号 Windows 路径参数中的反斜杠丢失，并修正管理员启动参数的 Windows 引号规则。
- 未知未来配置版本改为只读拒绝；v1 迁移改为验证和备份成功后再提交。
- 配置备份轮转错误不再静默，Recovery 可选择最近三份自动备份。
- 全局快捷键输入改为草稿提交，设置写入串行化，避免逐字符耗尽备份和响应乱序。
- 为主要弹窗补充焦点约束、Escape、焦点恢复和 ARIA 语义；任务卡主操作改为原生按钮。
- 新增可复现的便携版打包、产品元数据和 SHA-256 校验流程。
- 修复 Clippy 严格检查，并升级 `anyhow` 至修复不健全问题的版本。
- 补齐全局快捷键注册/注销的 Tauri 权限，修复 Release 构建中的 ACL 拒绝。
- 标题栏图标改为由 Vite 打包，避免便携版使用开发路径而显示破图。

### 验证

- 前端 48 项测试、Rust 48 项测试、TypeScript/Vite 构建、rustfmt、Clippy 和依赖审计纳入统一检查。

## 0.9.0 — 2026-09-02

### Added

- 接入 Tauri 全局快捷键插件，支持配置热键、注销和注册失败 fallback。
- Compact / Expanded 原生窗口尺寸切换。
- 按窗口模式持久化用户手动调整的尺寸到 `window-state.json`。
- 托盘菜单增加最近 5 个任务快速运行、新建任务和设置入口。
- Task Card 支持常用键盘动作，删除任务/分组增加确认。
- 任务拖放、跨分组移动与 group-aware `move_task` API。
- 所有任务、收藏、分组、设置和导入写入统一经过配置备份边界。
- 启动历史写入 `history.json`，Recent 从历史恢复，保留最近 200 条成功/失败记录。
- 设置页增加独立启动历史查看窗口，支持刷新、空状态和失败原因展示。
- Diagnostics 复制信息中的配置路径改为 `%APPDATA%\\ScriptLauncher\\config.json`，不暴露 Windows 用户名。
- Recovery 支持恢复最近自动备份和打开配置目录，并拒绝恢复不存在的备份槽位。
- 清理不再由 v2 `App.tsx` 使用的 v1 分页、旧弹层和旧任务卡片组件，避免死代码与失真测试。

### Changed

- 命令行任务入口暂时只开放后端已支持的 WSL；PowerShell/CMD 普通命令待独立 Command 模型。
- README 同步到 v2 任务启动台的信息架构和快捷键。
- 产品品牌更新为中文名「启动快捷运行」、英文名「RunTelos」，保留 `ScriptLauncher` 内部标识与数据目录兼容性。

## 0.1.0 - 2026-05-15

### Added

- 初始 Tauri + React 桌面应用
- `.bat` / `.cmd` / `.ps1` / `.py` / `.exe` 启动支持
- WSL 命令和 URL 启动项
- 配置文件持久化与自动分页
- 添加、重命名、删除、拖拽排序
- 运行状态反馈与错误详情
- 设置面板、浅色主题、窗口置顶、关闭到托盘
- 配置导入 / 导出
- 自定义应用图标与 release 构建
